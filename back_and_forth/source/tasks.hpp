#ifndef TASKS_HPP
#define TASKS_HPP

/**
 * Gibt die Pfeile an die allen gegebenen Pfeilen entsprechen -> Pfeile zusammenfassen
 * @param arr String Array mit Pfeilen in verschiedenen Richtungen
 * @return Zusammenfassung aller Pfeilrichtungen als String (Pfeil(e))
*/

std::string calculateArrowhead(std::vector<std::string> arr);

#endif
