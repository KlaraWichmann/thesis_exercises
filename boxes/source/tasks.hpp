#ifndef TASKS_HPP
#define TASKS_HPP

/**
 * Berechnet wie viele Boxen gepackt werden müssen fuer alle Teile
 * @param weights Array mit Gewichten der Einzelteile
 * @return Anzahl benoetigter Boxen
*/

int boxes (std::vector<int> weights);


#endif
