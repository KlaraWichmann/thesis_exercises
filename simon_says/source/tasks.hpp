#ifndef TASKS_HPP
#define TASKS_HPP

/**
 * Guckt, ob das zweite Array dem ersten bei einem Schritt folgt - 2. Array = 1.Array um 1 nach recht geshiftet
 * @param arr1 erstes Array -> Simon
 * @param arr2 zweites Array -> Array welches dem Simon-Array folgt
 * @return true: 2. Array folgt dem 1., sonst false
*/

bool simonSays (std::vector<int> arr1, std::vector<int> arr2);


#endif
