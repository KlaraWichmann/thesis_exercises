#ifndef TASKS_HPP
#define TASKS_HPP

/**
 * Transformation eines Arrays - ungerade Zahlen +2, gerade -2 -> n-mal
 * @param arr zu transformierendes Array
 * @param n Anzahl wie oft Transformation gemacht wird
 * @return transformiertes Array
*/

std::vector<int> evenOddTransform(std::vector<int> arr, int n);

#endif
