#ifndef TASKS_HPP
#define TASKS_HPP

/**
 * Tauscht Buchstaben, so dass ein String wieder geordnet ist
 * @param str String der veraendert werden soll
 * @return originaler String, wo immer paarweise die Zeichen getauscht wurden
*/

std::string unmix (std::string str);


#endif
